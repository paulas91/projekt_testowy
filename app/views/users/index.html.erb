<p style="color: green"><%= notice %></p>

<h1>Users</h1>

<div id="users">
  <% @users.each do |user| %>
    <%= link_to user.full_name, user_path(user), class: "btn btn-primary" %>
  <% end %>
</div>

<%= link_to "New user", new_user_path %><br>
